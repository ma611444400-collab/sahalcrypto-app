<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using built-in SDK theme to avoid external dependency linking errors -->
    <style name="Theme.MyApplication" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">#0B0E11</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">#0B0E11</item>
    </style>
</resources>
